{
    "name": "buggregator",
    "scripts": {
        "dev": "cd frontend && npm run dev",
        "generate": "cd frontend && npm run generate"
    },
    "devDependencies": {
        "lint-staged": "^13.1.0"
    }
}
